# Lambda runtime uses its own boto3 — these are for local dev/testing only
boto3>=1.35.0
botocore>=1.35.0

# For development purpose only - not for prod
pipdeptree>=2.23.0
pip-licenses>=5.0.0

# AWS CDK
aws-cdk-lib>=2.170.0
constructs>=10.3.0
pytest>=8.3.0
cdk-nag>=2.28.0
